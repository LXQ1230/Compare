# 项目进度状态 — 2026-07-28 (最终)

> 保存时间：会话结束时
> 下次继续：读取此文件了解当前进度和剩余工作
> HEAD: `c22db0b` | 工作树: 干净 | 分支: master

---

## 当前状态

| 指标 | 值 |
|------|-----|
| **总提交数** | 16 |
| **后端测试** | 72/72 passed |
| **前端测试** | 18/18 passed (1 file, `src/utils/__tests__/core.spec.ts`) |
| **生产构建** | `npm run build` 通过 |
| **启动方式** | `start.bat` (构建前端 → 启动 uvicorn :17890，挂载 dist/) |

---

## 提交历史

```
c22db0b fix: use Promise.withResolvers to await stream done event instead of compareFiles internal resolve
bb66a9c fix: process final NDJSON chunk in stream reader when done and data arrive together
8e6cc74 test: add 18 frontend unit tests for renderer, search, and exporters
6f34487 fix: add SPA static mount, J/K/V shortcuts, editClassifier with tests, and navigation buttons in toolbar
3c45bcc chore: add startup script, env config, and gitignore updates
60db5de feat: add all UI components — select page, report page with unified/split views, sidebar, search, export, version history, error display
bb4ae91 feat: add Pinia stores for compare, editor, search, view, and version
24e3cfb feat: add frontend utility layer — api client, indexeddb, storage, search, keybindings
45cebc3 feat: add autosave, version management, and file validation endpoints
40a50c4 feat: add POST /api/compare NDJSON streaming diff endpoint
c023db1 fix: use Diff_Timeout attribute instead of timeout kwarg in diff_main
8cbcf39 chore: save progress checkpoint — Tasks 1-4 backend complete, 12/12 tests pass
e3783f8 feat: scaffold Vue 3 + TypeScript + Vite frontend with router skeleton
260b6ff chore: add .gitignore and untrack __pycache__ files
55d1869 feat: scaffold Python backend with FastAPI, error handling, and health endpoint
33e0438 chore: initial commit with design and plan docs
```

---

## Tasks 1-19: 已完成 ✅ | Task 20: 未启动 ❌

---

## 已知问题（按优先级）

### 需用户验证
- **流式对比卡住**: 已修复两处 bug（reader 处理 + Promise 解析），需运行 `start.bat` 验证对比→跳转报告页流程

### P1 — 下次优先
- **J/K 快捷键未连线**: `ReportPage.vue` 中 `onNextChange/onPrevChange` 是空函数，需连线到 sidebar 变更列表跳转
- **前端测试覆盖率低**: 只有 1 个测试文件，需补充 stores/组件/editClassifier 测试

### P2 — 规格差距
- **编码确认弹窗**: 规格要求 chardet 置信度 <90% 弹 `EncodeDialog.vue`，当前未实现
- **资源警告**: `resource.ts estimateMemory()` 未被 SelectPage 调用
- **虚拟滚动**: 大文档全量 v-html 渲染，未按规格实现虚拟列表
- **SPA fallback**: 生产模式直接访问 `/report` 会 404

### P3
- Minimap 未独立为组件
- Task 20 E2E 测试

---

## 文件结构

```
Compare/
├── .env.example / .gitignore / start.bat
├── package.json / requirements.txt
├── vite.config.ts / tsconfig.json / tsconfig.node.json / env.d.ts / index.html
├── src/                          # 前端 (30 文件)
│   ├── main.ts / App.vue
│   ├── types/index.ts / router/index.ts
│   ├── stores/   (compare, editor, search, view, version)
│   ├── render/   (segmentRenderer, splitRenderer, editClassifier)
│   ├── export/   (exporters.ts)
│   ├── components/
│   │   ├── select-page/ (DropZone.vue)
│   │   └── report-page/ (Toolbar, Sidebar, UnifiedView, SplitView,
│   │         SearchBar, ExportDialog, VersionHistory, ErrorDisplay, ProgressHeader)
│   ├── utils/    (api.ts, indexeddb.ts, storage.ts, search.ts, resource.ts, keybindings.ts
│   │              + __tests__/core.spec.ts)
│   ├── views/    (SelectPage.vue, ReportPage.vue)
│   └── styles/   (variables.css, main.css)
├── src_backend/                  # 后端 (9 文件)
│   ├── main.py (260行) / errors.py / diff_engine.py / validators.py
│   ├── parsers/ (txt, docx, md)
│   ├── version_manager.py / autosave_manager.py
├── tests/                        # 后端测试
│   ├── unit/ (test_diff_engine, test_parser_*, test_validators, test_version_manager,
│   │         test_autosave_manager, test_health)
│   ├── integration/ (test_api, test_autosave_version_api)
│   └── fixtures/ (hello.txt, hello_gbk.txt, simple.docx, simple.md, sample_a.txt, sample_b.txt)
├── docs/superpowers/
│   ├── specs/2026-07-27-compare-design.md
│   ├── plans/2026-07-28-compare-implementation.md
│   └── 2026-07-28-progress.md (本文件)
└── dist/  (Vite 构建产物)
```

---

## 命令速查

```bash
# 一键启动
start.bat

# 开发模式
python -m uvicorn src_backend.main:app --host 127.0.0.1 --port 17890
npm run dev                # → http://localhost:5173

# 测试
pytest tests/ -v                           # 后端 72 测试
npx vitest run --config vite.config.ts     # 前端 18 测试

# 构建
npm run build
```
