# 2026-08-05 工作日志

## 标点归因三层防线（diff 语义修复，用户实测）

### 问题（用户实测 497有图.txt / 497有图修改.txt）
- 原「我聞如是」改「我。聞。如是。」，用户意图是「聞」前后各加「。」（2 处新增标点）
- DMP 输出 `DEL 聞 + ADD 。聞。`，把实词「聞」标成 mod（删除线+替换）——语义错误
- 根因：DMP 最小编辑不唯一，"替换实词"与"插入标点"数学等价，DMP 默认选前者；佛经句读场景应优先归因标点

### 方案：三层标点归因防线（前后端同步实现）
- **L1 标点移动**（已有 08-04）：DEL X + 纯标点间隔(≤8) + ADD X → ADD 标点 + X + DEL 标点
- **L2 标点包裹**（本次新增）：DEL X + ADD Y，X⊂Y(Y=P+X+Q) → ADD P + EQ X + ADD Q；Y⊂X → DEL P + EQ Y + DEL Q；两侧须纯标点（含汉字=真替换不重写）
- **L3 实词对齐兜底**（本次新增）：DEL X + ADD Y 紧邻，去标点后实词串相同 → 间隙对齐算法强制标点归因；与前后操作相邻会合成 mod 时放弃重写（保守）
- 收尾 `_merge_adjacent` 合并相邻同类操作

### 实现位置
- 后端 `src_backend/diff_engine.py`：`_resolve_punct_substring` / `_resolve_punct_alignment` / `_merge_adjacent`，diff_texts 内 L1→L2→L3→merge
- 前端 `src/render/unicode.ts`：`resolvePunctSubstring` / `resolvePunctAlignment` / `mergeAdjacent`，diffSafely 同样接入

### 测试与验证
- 后端 tests/unit/test_diff_engine.py：TestPunctSubstring(6) + TestPunctAlignment(7)，全量 107 过
- 前端 unicode.spec.ts：L2(4) + L3(6) + 编辑态场景(1)，全量 118 过
- 真实文件验证：63 万字 diff，9 处变更全部正确，mod 1→0；「聞」前后各 add「。」、舎衛國。在→舎衛。國在(L1)、删誦/删十/删句号等用户修改全部正确识别
- L3 在 DMP 正常路径几乎不触发（cleanupSemantic 已擅长保实词），采用规则函数直测

### 环境坑
- safe-delete 拒绝对相对路径删文件，须用绝对路径 rm
- vitest esbuild 编译失败时看报错定位语法问题（f-string/单引号嵌套）
- 注意：Edit 大块替换时 old_string 若含 def 行，new_string 必须完整保留，否则会误删函数定义（本次 diff_texts 定义被误删一次）

## 空白归因 W 规则（用户第二轮实测 497有图_WD句读结果.txt）

### 问题（用户实测 Word 句读结果）
- Word 句读把「行尾回车/空格 → 标点」作为常规操作，diff 把空白符当内容：
  1. 「卷第一\u3000\u3000」→「卷第一。」显示 mod（空格删除线），期望「一」后新增句号
  2. 「受\n」→「受。」显示回车删除标记，期望只显示「受」后加句号
  3. 「晝度樹\u3000城水木積喻\u3000善人」→「晝度樹。城。水。木積。喻善人」把「城水木積喻」整段标 mod
  4. 「十經。\u2003。初一日誦」两个句号中间空格——**修改版文件本身如此，非 diff bug**

### 方案（与三层标点归因组合为四层 + W）
- **L3 扩展**：_strip_sep/_split_sep 把空白符（\n\r\t、U+3000、U+2000-200A、NBSP、BOM 等）当分隔符 → 实词串相同判定成立
- **W 规则** `_resolve_whitespace`：DEL 纯空白 + ADD 纯标点 → 折叠为 ADD 标点（空白删除隐藏）；孤立 DEL 纯空白 → 隐藏
- 调用链：L1→L2→L3→W→mergeAdjacent（前后端同步）
- B 侧重构一致（编辑模式安全）；仅 A 侧丢空白（空白非内容，无影响）

### 验证
- 后端 113 全过（新增 TestWhitespaceAttribution 6 用例）+ 前端 122 全过（新增 W describe 4 用例）
- 真实文件（63 万字，diff 87s）：mod 2341→44（-98%，剩余为真替换）；问题 1/2/3 全部正确，问题 4 如实显示
- TS 类型检查 0 错误

## 当前进度状态（提交前快照）
- **diff 语义核心已收官**：L1 标点移动 + L2 标点包裹 + L3 实词对齐 + W 空白归因，前后端同步，真实文件 mod 2341→44
- **审查修复已落地**：版本历史三处 P1（UI 入口/空内容保存/恢复不应用）+ /api/compare 内存 DoS 防护 + restoreFromDraft 未 saveMeta 等 P2，审计脚本 scripts/audit_api_test.py
- **待办（下一轮）**：版本集成/压缩历史/编辑态搜索/大文档侧边栏/硬刷新恢复 5 项测试缺口；编辑态 scrollToActiveMatch 走 CM 通道；resetToOriginal 清理草稿
- **Git**：本次提交为标点归因 + 审查修复合并提交，随后推送 GitHub 远程（git@github.com:LXQ1230/Compare.git）
